# ruby projects need a Gemfile to:
   # list source for gems
   # list gems i want

source 'https://rubygems.org'

gem 'sinatra' #for server functionality
gem 'json' # has to json like excel to csv
gem 'httparty'
gem 'pry' # to pry into code
